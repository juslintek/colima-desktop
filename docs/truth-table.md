# Colima Desktop — Action/Outcome Truth Table

`truth-table.csv` enumerates **7,996 grounded action→outcome rows** (generated by
`scripts/gen-truth-table.py`, run before UI exploration). Each row is tied to a real
view, control/`accessibilityIdentifier`, backend call, and the test that verifies it.

Columns: `area, precondition, action, expected_outcome, backend_call, verified_by`.

## What dominates the space (and why >1000)

| Area | Rows | Notes |
|------|------|-------|
| Configuration/VMConfigCombo | 7,776 | Full cross of every distinct `colima` VM config: vmType×cpuType×mountType×arch×runtime (3⁵) × the 5 toggles rosetta/nestedVirt/binfmt/inotify/autoActivate (2⁵). |
| Containers (+create/detail) | ~43 | per-action × per-state, popular images, detail tabs |
| Navigation / Dashboard | ~50 | every tab × VM state, dashboard actions, ResourceAdvisor |
| Images/Volumes/Networks | ~34 | CRUD × empty/populated |
| Kubernetes | ~21 | lifecycle, quick actions, resource tabs (real kubectl json) |
| Configuration (pickers/resources/actions) | ~28 | arch/runtime/portForwarder/networkMode/modelRunner, steppers, save/reset/yaml/mounts |
| Profiles/Machines/AI/Monitoring/RuntimeControls/Community/MenuBar/Onboarding/Update | ~44 | per domain |

The VM-config matrix carries **validity verdicts**: 2,592 valid, 5,184 invalid/ignored,
encoding the real constraints — e.g. `9p` unsupported on `vz`, `rosetta`/`nestedVirt`
require `vz` (nestedVirt needs M3+), `krunkit` pairs with the docker model runner.

## How it maps to automated tests
`verified_by` points at the suite that covers each row's mechanic:
`AppShellUITests, ColimaLifecycleUITests, ContainerManagementUITests,
ContainerImageConfigUITests, NativePerformanceConfigUITests, KubernetesLifecycleUITests,
ConfigurationUITests, VMConfigurationFlowUITests, MachinesUITests, MonitoringUITests,
RealBackendTests, MenuBarViewTests, InstallPromptUITests`.

The combinatorial config rows are covered representatively (each option/toggle individually +
key combos) by `NativePerformanceConfigUITests` rather than 7,776 literal UI runs — exhaustive
literal UI enumeration isn't feasible (XCUITest can't run on the host; the host machine can't run
nested colima). See `e2e-real-mode-execution.md` for that constraint.
